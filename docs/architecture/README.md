# Architecture

Architecture documentation describes the system's boundaries, major components,
relationships, constraints, and evolution. It should help an engineer reason
about change rather than mirror the directory tree.

Derived repositories should add an architecture overview when a real system
exists. This template does not fabricate components or diagrams. Until then, the
`decisions/` directory provides a stable location for recording choices whose
rationale must survive the original discussion.
